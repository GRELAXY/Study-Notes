# Linux 命令行
___
## 1. 文件与目录操作
### 常用命令
- `ls`: 用于列出目录内容，
  - `-a`:显示隐藏文件
  - `-l`:显示详细信息
- `cd`:切换目录
- `pwd`:显示当先路径
- `mkdir`:创建文件或者目录
  - `-p`:创建递归文件夹
例如:`mkdir -p test_ws/src/robot_slam` 
- `touch`:创建空文件或者更新时间戳

### 文件操作
- `cp`：复制文件或者目录
  - `-r`:递归复制
- `mv`:移动或者重命名
- `rm`：删除文件或者目录
    - `-r`:递归删除
    - `-f`:强制删除（谨慎使用）
- `cat`：查看文件内容
  - `-n`：显示行号
  - `more/less`：分页查看，适合大文件浏览

### 文件搜索与查看
- `head/tail`：查看文件头部/尾部内容
  - `-n`：指定行数
- `find`：搜索文件
  - `-name`：按名称
  - `-type`：按类型
例如 `find home - name "*.txt"可查找/home下所有的.txt文件

___
## 2. 系统信息与进程管理

### 系统信息查看
- `uname`: 显示系统信息
  - `-a`:显示全部，包括内核版本、主机名等，便于系统诊断
- `df`:查看磁盘空间，直观了解磁盘使用情况
  - `-h`:以人类可读的格式显示

### 进程监控与管理
- `top/htop`：实时监控系统进程与资源，交互式操作，可直观查看系统负载和进程状态
  - `ps`:查看进程状态
  - `aux`:显示所有用户进程，结合`grep`可快速定位特定进程
  - `kill`:终止进程
    - `-9`:强制停止
    - `-15`:==优先使用==，可避免数据丢失


### 系统操作命令
- `shutdown`:关机或者重启
  -`-h now`:立即关机
  - `-r +10`:10分钟之后重启，操作前==需保存工作==

___

## 3. 网络相关命令

### 网络测试与信息查看
- `ping`：测试网络联通性
  - `-c 4`:发送4次包，用于快速检测目标主机是否可达
- `ifconfig`:查看网络接口信息，需`root`权限

### 网络连接与文件下载
- `netstat`:显示网络连接状态
- `tunip`:显示监听端口，了解系统网络服务情况
- `curl`:下载文件或测试API
  - `-O` :保存文件
*例：`curl -O https://example.com/file`可下载指定URL的文件*

- `wget`:从网络下载文件
  - `- c`: 支持断点续传


___
## 4. 权限管理
### 权限修改与用户管理
- `chmod`:修改文件权限
  - `755`:所有者可读写执行，组用户和其他用户可读执行
- `chown`: 修改文件所有者，支持指定用户和组，确保文件归属正确


### 权限说明与注意事项
- `sudo`:以管理员权限执行
- 权限由r(读),w(写),x(执行)组成，数字分别为4,2,1,组合使用



___
 ## VIM的使用
 进入 vim 编辑器的命令：`vim file_name`



![alt text](176032617440255182.png)


___
## 补充

### shell 有关
- `echo $PATH`:打印环境变量
- `export`:临时添加编辑环境变量
- `~/.bashrc`：打开bashrc文件，永久添加(bashrc在主文件夹下，用Ctrl+H可显示隐藏文件)

### shell脚本
编写 script.sh,以# !/bin/bash开头和内容（如，`echo "Hello` world!"，即打印Hello world！），运行`chmod +x         script.sh &&./script.sh`输出结果